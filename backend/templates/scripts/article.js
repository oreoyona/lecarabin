const addCatBtn = document.getElementById('addCat');const new_cat_form = document.getElementById('cat_form');const sideForm = document.getElementById('aside_form');const cat_form_cancel_button = document.getElementById("cat_form_cancel_button");const cat_form_submit_btn = document.getElementById("cat_form_submit_btn");const tag_input = document.getElementById('tag_input');const tag_section = document.getElementById('tag-section');const add_tag_btn = document.getElementById('add-tag-btn');const tags = document.getElementsByClassName('tag');const delete_article_btn = document.getElementById('delete-article-btn')    addCatBtn.addEventListener('click', ()=>{            new_cat_form.style.display = 'flex';            sideForm.style.display = 'none';    })    cat_form_submit_btn.classList.add('btn-primary');    cat_form_cancel_button.addEventListener("click", ()=>{            new_cat_form.style.display = 'none';            sideForm.style.display = 'block';    })    tag_input.addEventListener('keypress', (event)=>{        if(event.code == 'Enter' && tag_input.value.length >= 3){                const node = document.createElement("span");                node.classList.add('tag');                node.innerText = `${tag_input.value}`                tag_section.appendChild(node)                tag_input.value = '';                loadTags()            }    })    add_tag_btn.addEventListener('click', ()=>{            if (tag_input.value.length >= 3) {                const node = document.createElement('span');                node.classList.add('tag');                node.innerText = `${tag_input.value}`;                tag_section.appendChild(node);                tag_input.value = '';                loadTags()            }    })    delete_article_btn.addEventListener('click', ()=>{        const url = findTheUrl()    })    function loadTags(){            const list = document.getElementsByClassName('tag');            const listEl = [];            for (let el of list)listEl.push(el)            listEl[0].classList.add('first_tag');            listEl.length>2?listEl[1].classList.add('second_tag'): null;            listEl.length>3?listEl[2].classList.add('third_tag'):null;    }    async function load_data(){        const res = await fetch("./api/articles");        const jsonData = await res.json();    }function findTheUrl(url = window.location.href){        let returnString = '';        const sep = url.split('/')        let lat_char = sep[sep.length - 1]        lat_char == 'new-article'?returnString = lat_char: returnString = 'edit-post';        return returnString}function deleteArticle(post_id){        fetch('/api/edit-article', )}